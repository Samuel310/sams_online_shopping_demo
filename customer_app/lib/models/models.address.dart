class ShippingAddress{
  String fullname, address, city, pincode, country, state, phone, id;
  ShippingAddress({
    this.id,
    this.address,
    this.city,
    this.country,
    this.fullname,
    this.phone,
    this.pincode,
    this.state
  });
}